var typed = new Typed(".text-mbclab", {
  strings :["MBC LAB ?"],
  typeSpeed: 130,
  backSpeed: 100,
  backDelay: 900,
  loop:true
})

var typed = new Typed(".div-cs", {
  strings :["CYBER SECURITY"],
  typeSpeed: 130,
  backSpeed: 100,
  backDelay: 900,
  loop:true
})

var typed = new Typed(".div-bd", {
  strings :["BIG DATA"],
  typeSpeed: 130,
  backSpeed: 100,
  backDelay: 900,
  loop:true
})

var typed = new Typed(".div-gt", {
  strings :["GAME TECH"],
  typeSpeed: 130,
  backSpeed: 100,
  backDelay: 900,
  loop:true
})

var typed = new Typed(".div-gis", {
  strings :["GEOGRAPHIC INFORMATION SYSTEM"],
  typeSpeed: 130,
  backSpeed: 100,
  backDelay: 900,
  loop:true
})

